# Readiness Trends

Snapshots are stored under `.evidence_graph/snapshots/`.

The current and previous snapshots are compared by control-state transitions:

- ready -> degraded
- non-ready -> ready (improved)
- ready-added and ready-removed sets
