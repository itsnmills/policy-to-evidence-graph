# Evidence Rules

A control is ready when all required evidence types are present and not stale.

Staleness:

- evidence expiry if present
- control-level stale threshold override
- default criticality thresholds from project config
